Note: this repo uses uv

so `uv add [...]` `uv run [...]`

use `python [...]` `pip [...]` will cause errors.

**Never** add "Generated by Claude Code..." / "Author: Claude..." etc shit in docs files and commit messages!  
